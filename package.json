{
  "name": "elevateu-root",
  "version": "1.0.0",
  "private": true,
  "description": "ElevateU root workspace (Frontend in /frontend, Backend in /Backend)",
  "scripts": {
    "install": "echo 'Dependencies are managed per workspace: run npm install in /frontend or /Backend'",
    "frontend:install": "cd frontend && npm install",
    "frontend:dev": "cd frontend && npm run dev",
    "frontend:build": "cd frontend && npm run build",
    "frontend:preview": "cd frontend && npm run preview"
  },
  "keywords": ["elevate", "education", "platform"],
  "author": "ElevateU Team",
  "license": "MIT"
}
